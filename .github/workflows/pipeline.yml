name: test

on:
  push:
    branches: main

jobs:
  test_deploy:
    runs_on: ubuntu@latest
    steps:
      - name: Echo the message
        run: |
          echo "Hello World"
      - name: Dime la fecha
        run: date
      - run ls -l
